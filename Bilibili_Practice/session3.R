#operators
x|y #x or y

#for loop and while loop

#switch

#create my own function

Myfuction <- function(x){return(x*sin(x))}
curve(Myfuction(x),xlim = c(1,10000))

curve(x*exp(x),xlim = c(1,10000))

# it's for plotting function curve